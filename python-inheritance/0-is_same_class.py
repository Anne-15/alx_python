"""
Represents a function that checks subclass.

"""
def is_same_class(obj, a_class):
    """
    Represents a function that checks subclass.

    """
    if isinstance(obj, a_class):
        return True
    else:
        return False